#include "Demo.h"

int main(void)
{
  // This is just a placeholder main function.
  // In a real application, you would call run_demo_1 or run_demo_2 as needed.
  int result = run_demo_1(1, 1) + run_demo_2(2, 2);
  printf("Result: %d\n", result);
  return 0;
}